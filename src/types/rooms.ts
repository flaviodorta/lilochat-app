export interface Room {
  created_at: string;
  id: string;
  name: string;
  user_id: string;
  video_is_playing: boolean;
  video_thumbnail_url: string;
  video_time: number;
  video_url: string;
}
